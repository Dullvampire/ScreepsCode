if (!Game.taskManager) {
    Game.taskManager = require('taskManager');
}

if (!Memory.tasks) {
    Memory.tasks = {};
}

if (!Memory.taskNumber) {
    Memory.taskNumber = 0;
}

module.exports.loop = function () {

};
